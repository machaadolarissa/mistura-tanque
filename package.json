{
  "name": "mistura-tanque-agro",
  "version": "1.1.0",
  "description": "Verificador web de compatibilidade de mistura no tanque + pH da água (IoT simulado)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "sensor": "node simulador/sensor.js",
    "test": "node --test tests/*.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  }
}
